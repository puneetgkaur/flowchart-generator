#include <stdio.h>

void main(int argc,char *argv[])
{
fprintf(fpPathFile,"1-");
   int a,b,c;
   a=atoi(argv[1]);
   b=atoi(argv[2]);
   c=atoi(argv[3]);
   if((a>b)&&(a>b))
   {
fprintf(fpPathFile,"2-");
   printf("%d",a);
   }
   else if(b>c)
   {
fprintf(fpPathFile,"3-");
   printf("%d",b);
   }
   else
   {
fprintf(fpPathFile,"4-");
   printf("%d",c);
   }
   
}
